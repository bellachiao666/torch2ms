--- pytorch+++ mindspore@@ -1,6 +1,9 @@-import torch
-import torch.nn as nn
-import torch.nn.functional as F
+import mindspore as ms
+import mindspore.nn as msnn
+import mindspore.ops as msops
+import mindspore.mint as mint
+from mindspore.mint import nn, ops
+# import torch.nn as nn
 from time import time
 import numpy as np
 
@@ -34,9 +37,9 @@     """
     B, N, _ = src.shape
     _, M, _ = dst.shape
-    dist = -2 * torch.matmul(src, dst.permute(0, 2, 1))
-    dist += torch.sum(src ** 2, -1).view(B, N, 1)
-    dist += torch.sum(dst ** 2, -1).view(B, 1, M)
+    dist = -2 * mint.matmul(src, dst.permute(0, 2, 1))
+    dist += mint.sum(src ** 2, -1).view(B, N, 1)
+    dist += mint.sum(dst ** 2, -1).view(B, 1, M)
     return dist
 
 
@@ -55,7 +58,7 @@     view_shape[1:] = [1] * (len(view_shape) - 1)
     repeat_shape = list(idx.shape)
     repeat_shape[0] = 1
-    batch_indices = torch.arange(B, dtype=torch.long).to(device).view(view_shape).repeat(repeat_shape)
+    batch_indices = mint.arange(B, dtype=ms.int64).to(device).view(view_shape).repeat(repeat_shape)
     new_points = points[batch_indices, idx, :]
     return new_points
 
@@ -70,17 +73,17 @@     """
     device = xyz.device
     B, N, C = xyz.shape
-    centroids = torch.zeros(B, npoint, dtype=torch.long).to(device)
-    distance = torch.ones(B, N).to(device) * 1e10
-    farthest = torch.randint(0, N, (B,), dtype=torch.long).to(device)
-    batch_indices = torch.arange(B, dtype=torch.long).to(device)
+    centroids = mint.zeros(B, npoint, dtype=ms.int64).to(device)
+    distance = mint.ones(B, N).to(device) * 1e10
+    farthest = mint.randint(0, N, (B,), dtype=ms.int64).to(device)
+    batch_indices = mint.arange(B, dtype=ms.int64).to(device)
     for i in range(npoint):
         centroids[:, i] = farthest
         centroid = xyz[batch_indices, farthest, :].view(B, 1, 3)
-        dist = torch.sum((xyz - centroid) ** 2, -1)
+        dist = mint.sum((xyz - centroid) ** 2, -1)
         mask = dist < distance
         distance[mask] = dist[mask]
-        farthest = torch.max(distance, -1)[1]
+        farthest = mint.max(distance, -1)[1]
     return centroids
 
 
@@ -97,7 +100,7 @@     device = xyz.device
     B, N, C = xyz.shape
     _, S, _ = new_xyz.shape
-    group_idx = torch.arange(N, dtype=torch.long).to(device).view(1, 1, N).repeat([B, S, 1])
+    group_idx = mint.arange(N, dtype=ms.int64).to(device).view(1, 1, N).repeat([B, S, 1])
     sqrdists = square_distance(new_xyz, xyz)
     group_idx[sqrdists > radius ** 2] = N
     group_idx = group_idx.sort(dim=-1)[0][:, :, :nsample]
@@ -129,7 +132,7 @@ 
     if points is not None:
         grouped_points = index_points(points, idx)
-        new_points = torch.cat([grouped_xyz_norm, grouped_points], dim=-1) # [B, npoint, nsample, C+D]
+        new_points = mint.cat([grouped_xyz_norm, grouped_points], dim=-1) # [B, npoint, nsample, C+D]
     else:
         new_points = grouped_xyz_norm
     if returnfps:
@@ -149,23 +152,23 @@     """
     device = xyz.device
     B, N, C = xyz.shape
-    new_xyz = torch.zeros(B, 1, C).to(device)
+    new_xyz = mint.zeros(B, 1, C).to(device)
     grouped_xyz = xyz.view(B, 1, N, C)
     if points is not None:
-        new_points = torch.cat([grouped_xyz, points.view(B, 1, N, -1)], dim=-1)
+        new_points = mint.cat([grouped_xyz, points.view(B, 1, N, -1)], dim=-1)
     else:
         new_points = grouped_xyz
     return new_xyz, new_points
 
 
-class PointNetSetAbstraction(nn.Module):
+class PointNetSetAbstraction(msnn.Cell):
     def __init__(self, npoint, radius, nsample, in_channel, mlp, group_all):
         super(PointNetSetAbstraction, self).__init__()
         self.npoint = npoint
         self.radius = radius
         self.nsample = nsample
-        self.mlp_convs = nn.ModuleList()
-        self.mlp_bns = nn.ModuleList()
+        self.mlp_convs = msnn.CellList()
+        self.mlp_bns = msnn.CellList()
         last_channel = in_channel
         for out_channel in mlp:
             self.mlp_convs.append(nn.Conv2d(last_channel, out_channel, 1))
@@ -173,7 +176,7 @@             last_channel = out_channel
         self.group_all = group_all
 
-    def forward(self, xyz, points):
+    def construct(self, xyz, points):
         """
         Input:
             xyz: input points position data, [B, C, N]
@@ -195,24 +198,24 @@         new_points = new_points.permute(0, 3, 2, 1) # [B, C+D, nsample,npoint]
         for i, conv in enumerate(self.mlp_convs):
             bn = self.mlp_bns[i]
-            new_points =  F.relu(bn(conv(new_points)))
-
-        new_points = torch.max(new_points, 2)[0]
+            new_points =  nn.functional.relu(bn(conv(new_points)))
+
+        new_points = mint.max(new_points, 2)[0]
         new_xyz = new_xyz.permute(0, 2, 1)
         return new_xyz, new_points
 
 
-class PointNetSetAbstractionMsg(nn.Module):
+class PointNetSetAbstractionMsg(msnn.Cell):
     def __init__(self, npoint, radius_list, nsample_list, in_channel, mlp_list):
         super(PointNetSetAbstractionMsg, self).__init__()
         self.npoint = npoint
         self.radius_list = radius_list
         self.nsample_list = nsample_list
-        self.conv_blocks = nn.ModuleList()
-        self.bn_blocks = nn.ModuleList()
+        self.conv_blocks = msnn.CellList()
+        self.bn_blocks = msnn.CellList()
         for i in range(len(mlp_list)):
-            convs = nn.ModuleList()
-            bns = nn.ModuleList()
+            convs = msnn.CellList()
+            bns = msnn.CellList()
             last_channel = in_channel + 3
             for out_channel in mlp_list[i]:
                 convs.append(nn.Conv2d(last_channel, out_channel, 1))
@@ -221,7 +224,7 @@             self.conv_blocks.append(convs)
             self.bn_blocks.append(bns)
 
-    def forward(self, xyz, points):
+    def construct(self, xyz, points):
         """
         Input:
             xyz: input points position data, [B, C, N]
@@ -245,7 +248,7 @@             grouped_xyz -= new_xyz.view(B, S, 1, C)
             if points is not None:
                 grouped_points = index_points(points, group_idx)
-                grouped_points = torch.cat([grouped_points, grouped_xyz], dim=-1)
+                grouped_points = mint.cat([grouped_points, grouped_xyz], dim=-1)
             else:
                 grouped_points = grouped_xyz
 
@@ -253,27 +256,27 @@             for j in range(len(self.conv_blocks[i])):
                 conv = self.conv_blocks[i][j]
                 bn = self.bn_blocks[i][j]
-                grouped_points =  F.relu(bn(conv(grouped_points)))
-            new_points = torch.max(grouped_points, 2)[0]  # [B, D', S]
+                grouped_points =  nn.functional.relu(bn(conv(grouped_points)))
+            new_points = mint.max(grouped_points, 2)[0]  # [B, D', S]
             new_points_list.append(new_points)
 
         new_xyz = new_xyz.permute(0, 2, 1)
-        new_points_concat = torch.cat(new_points_list, dim=1)
+        new_points_concat = mint.cat(new_points_list, dim=1)
         return new_xyz, new_points_concat
 
 
-class PointNetFeaturePropagation(nn.Module):
+class PointNetFeaturePropagation(msnn.Cell):
     def __init__(self, in_channel, mlp):
         super(PointNetFeaturePropagation, self).__init__()
-        self.mlp_convs = nn.ModuleList()
-        self.mlp_bns = nn.ModuleList()
+        self.mlp_convs = msnn.CellList()
+        self.mlp_bns = msnn.CellList()
         last_channel = in_channel
         for out_channel in mlp:
             self.mlp_convs.append(nn.Conv1d(last_channel, out_channel, 1))
             self.mlp_bns.append(nn.BatchNorm1d(out_channel))
             last_channel = out_channel
 
-    def forward(self, xyz1, xyz2, points1, points2):
+    def construct(self, xyz1, xyz2, points1, points2):
         """
         Input:
             xyz1: input points position data, [B, C, N]
@@ -298,19 +301,19 @@             dists, idx = dists[:, :, :3], idx[:, :, :3]  # [B, N, 3]
 
             dist_recip = 1.0 / (dists + 1e-8)
-            norm = torch.sum(dist_recip, dim=2, keepdim=True)
+            norm = mint.sum(dist_recip, dim=2, keepdim=True)
             weight = dist_recip / norm
-            interpolated_points = torch.sum(index_points(points2, idx) * weight.view(B, N, 3, 1), dim=2)
+            interpolated_points = mint.sum(index_points(points2, idx) * weight.view(B, N, 3, 1), dim=2)
 
         if points1 is not None:
             points1 = points1.permute(0, 2, 1)
-            new_points = torch.cat([points1, interpolated_points], dim=-1)
+            new_points = mint.cat([points1, interpolated_points], dim=-1)
         else:
             new_points = interpolated_points
 
         new_points = new_points.permute(0, 2, 1)
         for i, conv in enumerate(self.mlp_convs):
             bn = self.mlp_bns[i]
-            new_points = F.relu(bn(conv(new_points)))
+            new_points = nn.functional.relu(bn(conv(new_points)))
         return new_points
 
