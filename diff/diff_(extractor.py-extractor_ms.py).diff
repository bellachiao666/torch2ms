--- pytorch+++ mindspore@@ -1,5 +1,5 @@-import torch
 from torch import nn
+from mindspore.mint import nn, ops
 
 def exists(val):
     return val is not None
@@ -15,7 +15,7 @@         return tuple(map(fn, val))
     return fn(val)
 
-class Extractor(nn.Module):
+class Extractor(nn.Cell):
     def __init__(
         self,
         vit,
