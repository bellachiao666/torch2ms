--- pytorch+++ mindspore@@ -1,3 +1,8 @@+import mindspore as ms
+import mindspore.nn as msnn
+import mindspore.ops as msops
+import mindspore.mint as mint
+from mindspore.mint import nn, ops
 from dataclasses import field
 from typing import Dict, Optional, cast
 
@@ -17,7 +22,7 @@     sizes: dict = field(default_factory=dict)
 
     def setup(self):
-        self.reducer = ReduceMixin(self.pattern, self.reduction, **self.sizes)
+        self.reducer = ReduceMixin(self.pattern, self.reduction, **self.sizes)  # 存在 *args/**kwargs，未转换，需手动确认参数映射;
 
     def __call__(self, input):
         return self.reducer._apply_recipe(input)
@@ -28,7 +33,7 @@     sizes: dict = field(default_factory=dict)
 
     def setup(self):
-        self.rearranger = RearrangeMixin(self.pattern, **self.sizes)
+        self.rearranger = RearrangeMixin(self.pattern, **self.sizes)  # 存在 *args/**kwargs，未转换，需手动确认参数映射;
 
     def __call__(self, input):
         return self.rearranger._apply_recipe(input)
