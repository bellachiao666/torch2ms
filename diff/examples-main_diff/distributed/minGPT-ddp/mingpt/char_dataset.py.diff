--- pytorch+++ mindspore@@ -1,5 +1,9 @@-import torch
-from torch.utils.data import Dataset
+import mindspore as ms
+import mindspore.nn as msnn
+import mindspore.ops as msops
+import mindspore.mint as mint
+from mindspore.mint import nn, ops
+# from torch.utils.data import Dataset
 import fsspec
 from dataclasses import dataclass
 
@@ -14,6 +18,7 @@     train_split: float = None
     truncate: float = 1.0
 
+# 'torch.utils.data.Dataset' 未在映射表(api_mapping_out_excel.json)中找到，需手动确认;
 class CharDataset(Dataset):
 
     def __init__(self, data_cfg: DataConfig): #data_path: str, block_size):
@@ -38,6 +43,6 @@         chunk = self.data[idx:idx + self.block_size + 1]
         # encode every character to an integer
         dix = [self.stoi[s] for s in chunk]
-        x = torch.tensor(dix[:-1], dtype=torch.long)
-        y = torch.tensor(dix[1:], dtype=torch.long)
+        x = ms.Tensor(dix[:-1], dtype=ms.int64)
+        y = ms.Tensor(dix[1:], dtype=ms.int64)
         return x, y
