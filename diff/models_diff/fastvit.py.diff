--- pytorch+++ mindspore@@ -1,3 +1,8 @@+import mindspore as ms
+import mindspore.nn as msnn
+import mindspore.ops as msops
+import mindspore.mint as mint
+from mindspore.mint import nn, ops
 # FastViT for PyTorch
 #
 # Original implementation and weights from https://github.com/apple/ml-fastvit
@@ -9,8 +14,8 @@ from functools import partial
 from typing import List, Optional, Tuple, Type, Union
 
-import torch
-import torch.nn as nn
+# import torch
+# import torch.nn as nn
 
 from timm.data import IMAGENET_DEFAULT_MEAN, IMAGENET_DEFAULT_STD, OPENAI_CLIP_MEAN, OPENAI_CLIP_STD
 from timm.layers import (
@@ -40,7 +45,7 @@         return channels // group_size
 
 
-class MobileOneBlock(nn.Module):
+class MobileOneBlock(msnn.Cell):
     """MobileOne building block.
 
     This block has a multi-branched architecture at train-time
@@ -63,7 +68,7 @@             use_act: bool = True,
             use_scale_branch: bool = True,
             num_conv_branches: int = 1,
-            act_layer: Type[nn.Module] = nn.GELU,
+            act_layer: Type[msnn.Cell] = nn.GELU,
             device=None,
             dtype=None,
     ) -> None:
@@ -94,7 +99,7 @@         self.num_conv_branches = num_conv_branches
 
         # Check if SE-ReLU is requested
-        self.se = SqueezeExcite(out_chs, rd_divisor=1, **dd) if use_se else nn.Identity()
+        self.se = SqueezeExcite(out_chs, rd_divisor=1, **dd) if use_se else msnn.Identity()
 
         if inference_mode:
             self.reparam_conv = create_conv2d(
@@ -119,7 +124,7 @@ 
             # Re-parameterizable conv branches
             if num_conv_branches > 0:
-                self.conv_kxk = nn.ModuleList([
+                self.conv_kxk = msnn.CellList([
                     ConvNormAct(
                         self.in_chs,
                         self.out_chs,
@@ -146,9 +151,9 @@                     **dd,
                 )
 
-        self.act = act_layer() if use_act else nn.Identity()
-
-    def forward(self, x: torch.Tensor) -> torch.Tensor:
+        self.act = act_layer() if use_act else msnn.Identity()
+
+    def construct(self, x: ms.Tensor) -> ms.Tensor:
         """Apply forward pass."""
         # Inference mode forward pass.
         if self.reparam_conv is not None:
@@ -208,7 +213,7 @@ 
         self.inference_mode = True
 
-    def _get_kernel_bias(self) -> Tuple[torch.Tensor, torch.Tensor]:
+    def _get_kernel_bias(self) -> Tuple[ms.Tensor, ms.Tensor]:
         """Method to obtain re-parameterized kernel and bias.
         Reference: https://github.com/DingXiaoH/RepVGG/blob/main/repvgg.py#L83
 
@@ -222,7 +227,7 @@             kernel_scale, bias_scale = self._fuse_bn_tensor(self.conv_scale)
             # Pad scale branch kernel to match conv branch kernel size.
             pad = self.kernel_size // 2
-            kernel_scale = torch.nn.functional.pad(kernel_scale, [pad, pad, pad, pad])
+            kernel_scale = nn.functional.pad(kernel_scale, [pad, pad, pad, pad])
 
         # get weights and bias of skip branch
         kernel_identity = 0
@@ -245,8 +250,8 @@ 
     def _fuse_bn_tensor(
             self,
-            branch: Union[nn.Sequential, nn.BatchNorm2d]
-    ) -> Tuple[torch.Tensor, torch.Tensor]:
+            branch: Union[msnn.SequentialCell, nn.BatchNorm2d]
+    ) -> Tuple[ms.Tensor, ms.Tensor]:
         """Method to fuse batchnorm layer with preceding conv layer.
         Reference: https://github.com/DingXiaoH/RepVGG/blob/main/repvgg.py#L95
 
@@ -267,7 +272,7 @@             assert isinstance(branch, nn.BatchNorm2d)
             if not hasattr(self, "id_tensor"):
                 input_dim = self.in_chs // self.groups
-                kernel_value = torch.zeros(
+                kernel_value = mint.zeros(
                     (self.in_chs, input_dim, self.kernel_size, self.kernel_size),
                     dtype=branch.weight.dtype,
                     device=branch.weight.device,
@@ -288,7 +293,7 @@         return kernel * t, beta - running_mean * gamma / std
 
 
-class ReparamLargeKernelConv(nn.Module):
+class ReparamLargeKernelConv(msnn.Cell):
     """Building Block of RepLKNet
 
     This class defines overparameterized large kernel conv block
@@ -306,7 +311,7 @@             group_size: int,
             small_kernel: Optional[int] = None,
             use_se: bool = False,
-            act_layer: Optional[nn.Module] = None,
+            act_layer: Optional[msnn.Cell] = None,
             inference_mode: bool = False,
             device=None,
             dtype=None,
@@ -367,11 +372,11 @@                     apply_act=False,
                     **dd,
                 )
-        self.se = SqueezeExcite(out_chs, rd_ratio=0.25, **dd) if use_se else nn.Identity()
+        self.se = SqueezeExcite(out_chs, rd_ratio=0.25, **dd) if use_se else msnn.Identity()
         # FIXME output of this act was not used in original impl, likely due to bug
-        self.act = act_layer() if act_layer is not None else nn.Identity()
-
-    def forward(self, x: torch.Tensor) -> torch.Tensor:
+        self.act = act_layer() if act_layer is not None else msnn.Identity()
+
+    def construct(self, x: ms.Tensor) -> ms.Tensor:
         if self.reparam_conv is not None:
             out = self.reparam_conv(x)
         else:
@@ -382,7 +387,7 @@         out = self.act(out)
         return out
 
-    def get_kernel_bias(self) -> Tuple[torch.Tensor, torch.Tensor]:
+    def get_kernel_bias(self) -> Tuple[ms.Tensor, ms.Tensor]:
         """Method to obtain re-parameterized kernel and bias.
         Reference: https://github.com/DingXiaoH/RepLKNet-pytorch
 
@@ -394,8 +399,7 @@             small_k, small_b = self._fuse_bn(self.small_conv.conv, self.small_conv.bn)
             eq_b += small_b
             eq_k += nn.functional.pad(
-                small_k, [(self.kernel_size - self.small_kernel) // 2] * 4
-            )
+                small_k, [(self.kernel_size - self.small_kernel) // 2] * 4)
         return eq_k, eq_b
 
     def reparameterize(self) -> None:
@@ -425,7 +429,7 @@     def _fuse_bn(
             conv: nn.Conv2d,
             bn: nn.BatchNorm2d
-    ) -> Tuple[torch.Tensor, torch.Tensor]:
+    ) -> Tuple[ms.Tensor, ms.Tensor]:
         """Method to fuse batchnorm layer with conv layer.
 
         Args:
@@ -449,12 +453,12 @@ def convolutional_stem(
         in_chs: int,
         out_chs: int,
-        act_layer: Type[nn.Module] = nn.GELU,
+        act_layer: Type[msnn.Cell] = nn.GELU,
         inference_mode: bool = False,
         use_scale_branch: bool = True,
         device=None,
         dtype=None,
-) -> nn.Sequential:
+) -> msnn.SequentialCell:
     """Build convolutional stem with MobileOne blocks.
 
     Args:
@@ -466,7 +470,7 @@         nn.Sequential object with stem elements.
     """
     dd = {'device': device, 'dtype': dtype}
-    return nn.Sequential(
+    return msnn.SequentialCell(
         MobileOneBlock(
             in_chs=in_chs,
             out_chs=out_chs,
@@ -501,7 +505,7 @@     )
 
 
-class Attention(nn.Module):
+class Attention(msnn.Cell):
     """Multi-headed Self Attention module.
 
     Source modified from:
@@ -541,7 +545,7 @@         self.proj = nn.Linear(dim, dim, **dd)
         self.proj_drop = nn.Dropout(proj_drop)
 
-    def forward(self, x: torch.Tensor) -> torch.Tensor:
+    def construct(self, x: ms.Tensor) -> ms.Tensor:
         B, C, H, W = x.shape
         N = H * W
         x = x.flatten(2).transpose(-2, -1)  # (B, N, C)
@@ -556,7 +560,7 @@             x = torch.nn.functional.scaled_dot_product_attention(
                 q, k, v,
                 dropout_p=self.attn_drop.p if self.training else 0.,
-            )
+            )  # 'torch.nn.functional.scaled_dot_product_attention' 未在映射表(api_mapping_out_excel.json)中找到，需手动确认;
         else:
             q = q * self.scale
             attn = q @ k.transpose(-2, -1)
@@ -572,7 +576,7 @@         return x
 
 
-class PatchEmbed(nn.Module):
+class PatchEmbed(msnn.Cell):
     """Convolutional patch embedding layer."""
 
     def __init__(
@@ -581,7 +585,7 @@             stride: int,
             in_chs: int,
             embed_dim: int,
-            act_layer: Type[nn.Module] = nn.GELU,
+            act_layer: Type[msnn.Cell] = nn.GELU,
             lkc_use_act: bool = False,
             use_se: bool = False,
             inference_mode: bool = False,
@@ -599,7 +603,7 @@         """
         dd = {'device': device, 'dtype': dtype}
         super().__init__()
-        self.proj = nn.Sequential(
+        self.proj = msnn.SequentialCell(
             ReparamLargeKernelConv(
                 in_chs=in_chs,
                 out_chs=embed_dim,
@@ -624,12 +628,12 @@             )
         )
 
-    def forward(self, x: torch.Tensor) -> torch.Tensor:
+    def construct(self, x: ms.Tensor) -> ms.Tensor:
         x = self.proj(x)
         return x
 
 
-class LayerScale2d(nn.Module):
+class LayerScale2d(msnn.Cell):
     def __init__(
             self,
             dim: int,
@@ -640,13 +644,13 @@     ):
         super().__init__()
         self.inplace = inplace
-        self.gamma = nn.Parameter(init_values * torch.ones(dim, 1, 1, device=device, dtype=dtype))
-
-    def forward(self, x):
+        self.gamma = ms.Parameter(init_values * mint.ones(dim, 1, 1, device=device, dtype=dtype))
+
+    def construct(self, x):
         return x.mul_(self.gamma) if self.inplace else x * self.gamma
 
 
-class RepMixer(nn.Module):
+class RepMixer(msnn.Cell):
     """Reparameterizable token mixer.
 
     For more details, please refer to our paper:
@@ -710,9 +714,9 @@             if layer_scale_init_value is not None:
                 self.layer_scale = LayerScale2d(dim, layer_scale_init_value, **dd)
             else:
-                self.layer_scale = nn.Identity()
-
-    def forward(self, x: torch.Tensor) -> torch.Tensor:
+                self.layer_scale = msnn.Identity()
+
+    def construct(self, x: ms.Tensor) -> ms.Tensor:
         if self.reparam_conv is not None:
             x = self.reparam_conv(x)
         else:
@@ -733,7 +737,7 @@             w = self.mixer.id_tensor + self.layer_scale.gamma.unsqueeze(-1) * (
                 self.mixer.reparam_conv.weight - self.norm.reparam_conv.weight
             )
-            b = torch.squeeze(self.layer_scale.gamma) * (
+            b = mint.squeeze(self.layer_scale.gamma) * (
                 self.mixer.reparam_conv.bias - self.norm.reparam_conv.bias
             )
         else:
@@ -764,7 +768,7 @@         self.__delattr__("layer_scale")
 
 
-class ConvMlp(nn.Module):
+class ConvMlp(msnn.Cell):
     """Convolutional FFN Module."""
 
     def __init__(
@@ -772,7 +776,7 @@             in_chs: int,
             hidden_channels: Optional[int] = None,
             out_chs: Optional[int] = None,
-            act_layer: Type[nn.Module] = nn.GELU,
+            act_layer: Type[msnn.Cell] = nn.GELU,
             drop: float = 0.0,
             device=None,
             dtype=None,
@@ -804,13 +808,13 @@         self.drop = nn.Dropout(drop)
         self.apply(self._init_weights)
 
-    def _init_weights(self, m: nn.Module) -> None:
+    def _init_weights(self, m: msnn.Cell) -> None:
         if isinstance(m, nn.Conv2d):
             trunc_normal_(m.weight, std=0.02)
             if m.bias is not None:
-                nn.init.constant_(m.bias, 0)
-
-    def forward(self, x: torch.Tensor) -> torch.Tensor:
+                nn.init.constant_(m.bias, 0)  # 'torch.nn.init.constant_' 未在映射表(api_mapping_out_excel.json)中找到，需手动确认;
+
+    def construct(self, x: ms.Tensor) -> ms.Tensor:
         x = self.conv(x)
         x = self.fc1(x)
         x = self.act(x)
@@ -820,7 +824,7 @@         return x
 
 
-class RepConditionalPosEnc(nn.Module):
+class RepConditionalPosEnc(msnn.Cell):
     """Implementation of conditional positional encoding.
 
     For more details refer to paper:
@@ -888,7 +892,7 @@                 **dd,
             )
 
-    def forward(self, x: torch.Tensor) -> torch.Tensor:
+    def construct(self, x: ms.Tensor) -> ms.Tensor:
         if self.reparam_conv is not None:
             x = self.reparam_conv(x)
         else:
@@ -898,7 +902,7 @@     def reparameterize(self) -> None:
         # Build equivalent Id tensor
         input_dim = self.dim // self.groups
-        kernel_value = torch.zeros(
+        kernel_value = mint.zeros(
             (
                 self.dim,
                 input_dim,
@@ -923,14 +927,7 @@ 
         # Introduce reparam conv
         self.reparam_conv = nn.Conv2d(
-            self.dim,
-            self.dim_out,
-            kernel_size=self.spatial_shape,
-            stride=1,
-            padding=int(self.spatial_shape[0] // 2),
-            groups=self.groups,
-            bias=True,
-        )
+            self.dim, self.dim_out, kernel_size = self.spatial_shape, stride = 1, padding = int(self.spatial_shape[0] // 2), groups = self.groups, bias = True)
         self.reparam_conv.weight.data = w_final
         self.reparam_conv.bias.data = b_final
 
@@ -941,7 +938,7 @@         self.__delattr__("pos_enc")
 
 
-class RepMixerBlock(nn.Module):
+class RepMixerBlock(msnn.Cell):
     """Implementation of Metaformer block with RepMixer as token mixer.
 
     For more details on Metaformer structure, please refer to:
@@ -953,7 +950,7 @@             dim: int,
             kernel_size: int = 3,
             mlp_ratio: float = 4.0,
-            act_layer: Type[nn.Module] = nn.GELU,
+            act_layer: Type[msnn.Cell] = nn.GELU,
             proj_drop: float = 0.0,
             drop_path: float = 0.0,
             layer_scale_init_value: float = 1e-5,
@@ -994,16 +991,16 @@         if layer_scale_init_value is not None:
             self.layer_scale = LayerScale2d(dim, layer_scale_init_value, **dd)
         else:
-            self.layer_scale = nn.Identity()
-        self.drop_path = DropPath(drop_path) if drop_path > 0.0 else nn.Identity()
-
-    def forward(self, x):
+            self.layer_scale = msnn.Identity()
+        self.drop_path = DropPath(drop_path) if drop_path > 0.0 else msnn.Identity()
+
+    def construct(self, x):
         x = self.token_mixer(x)
         x = x + self.drop_path(self.layer_scale(self.mlp(x)))
         return x
 
 
-class AttentionBlock(nn.Module):
+class AttentionBlock(msnn.Cell):
     """Implementation of metaformer block with MHSA as token mixer.
 
     For more details on Metaformer structure, please refer to:
@@ -1014,8 +1011,8 @@             self,
             dim: int,
             mlp_ratio: float = 4.0,
-            act_layer: Type[nn.Module] = nn.GELU,
-            norm_layer: Type[nn.Module] = nn.BatchNorm2d,
+            act_layer: Type[msnn.Cell] = nn.GELU,
+            norm_layer: Type[msnn.Cell] = nn.BatchNorm2d,
             proj_drop: float = 0.0,
             drop_path: float = 0.0,
             layer_scale_init_value: float = 1e-5,
@@ -1041,8 +1038,8 @@         if layer_scale_init_value is not None:
             self.layer_scale_1 = LayerScale2d(dim, layer_scale_init_value, **dd)
         else:
-            self.layer_scale_1 = nn.Identity()
-        self.drop_path1 = DropPath(drop_path) if drop_path > 0.0 else nn.Identity()
+            self.layer_scale_1 = msnn.Identity()
+        self.drop_path1 = DropPath(drop_path) if drop_path > 0.0 else msnn.Identity()
 
         self.mlp = ConvMlp(
             in_chs=dim,
@@ -1054,16 +1051,16 @@         if layer_scale_init_value is not None:
             self.layer_scale_2 = LayerScale2d(dim, layer_scale_init_value, **dd)
         else:
-            self.layer_scale_2 = nn.Identity()
-        self.drop_path2 = DropPath(drop_path) if drop_path > 0.0 else nn.Identity()
-
-    def forward(self, x):
+            self.layer_scale_2 = msnn.Identity()
+        self.drop_path2 = DropPath(drop_path) if drop_path > 0.0 else msnn.Identity()
+
+    def construct(self, x):
         x = x + self.drop_path1(self.layer_scale_1(self.token_mixer(self.norm(x))))
         x = x + self.drop_path2(self.layer_scale_2(self.mlp(x)))
         return x
 
 
-class FastVitStage(nn.Module):
+class FastVitStage(msnn.Cell):
     def __init__(
             self,
             dim: int,
@@ -1074,11 +1071,11 @@             se_downsample: bool = False,
             down_patch_size: int = 7,
             down_stride: int = 2,
-            pos_emb_layer: Optional[nn.Module] = None,
+            pos_emb_layer: Optional[msnn.Cell] = None,
             kernel_size: int = 3,
             mlp_ratio: float = 4.0,
-            act_layer: Type[nn.Module] = nn.GELU,
-            norm_layer: Type[nn.Module] = nn.BatchNorm2d,
+            act_layer: Type[msnn.Cell] = nn.GELU,
+            norm_layer: Type[msnn.Cell] = nn.BatchNorm2d,
             proj_drop_rate: float = 0.0,
             drop_path_rate: Union[List[float], float] = 0.0,
             layer_scale_init_value: Optional[float] = 1e-5,
@@ -1120,12 +1117,12 @@             )
         else:
             assert dim == dim_out
-            self.downsample = nn.Identity()
+            self.downsample = msnn.Identity()
 
         if pos_emb_layer is not None:
             self.pos_emb = pos_emb_layer(dim_out, inference_mode=inference_mode, **dd)
         else:
-            self.pos_emb = nn.Identity()
+            self.pos_emb = msnn.Identity()
 
         blocks = []
         for block_idx in range(depth):
@@ -1156,9 +1153,9 @@                 raise ValueError(
                     "Token mixer type: {} not supported".format(token_mixer_type)
                 )
-        self.blocks = nn.Sequential(*blocks)
-
-    def forward(self, x):
+        self.blocks = msnn.SequentialCell(*blocks)
+
+    def construct(self, x):
         x = self.downsample(x)
         x = self.pos_emb(x)
         if self.grad_checkpointing and not torch.jit.is_scripting():
@@ -1168,7 +1165,7 @@         return x
 
 
-class FastVit(nn.Module):
+class FastVit(msnn.Cell):
     fork_feat: torch.jit.Final[bool]
 
     """
@@ -1186,7 +1183,7 @@             se_downsamples: Tuple[bool, ...] = (False, False, False, False),
             repmixer_kernel_size: int = 3,
             num_classes: int = 1000,
-            pos_embs: Tuple[Optional[nn.Module], ...] = (None,) * 4,
+            pos_embs: Tuple[Optional[msnn.Cell], ...] = (None,) * 4,
             down_patch_size: int = 7,
             down_stride: int = 2,
             drop_rate: float = 0.0,
@@ -1198,8 +1195,8 @@             fork_feat: bool = False,
             cls_ratio: float = 2.0,
             global_pool: str = 'avg',
-            norm_layer: Type[nn.Module] = nn.BatchNorm2d,
-            act_layer: Type[nn.Module] = nn.GELU,
+            norm_layer: Type[msnn.Cell] = nn.BatchNorm2d,
+            act_layer: Type[msnn.Cell] = nn.GELU,
             inference_mode: bool = False,
             device=None,
             dtype=None,
@@ -1254,7 +1251,7 @@             if downsample:
                 scale *= 2
             self.feature_info += [dict(num_chs=prev_dim, reduction=4 * scale, module=f'stages.{i}')]
-        self.stages = nn.Sequential(*stages)
+        self.stages = msnn.SequentialCell(*stages)
         self.num_stages = len(self.stages)
         self.num_features = self.head_hidden_size = prev_dim
 
@@ -1269,7 +1266,7 @@                     """For RetinaNet, `start_level=1`. The first norm layer will not used.
                     cmd: `FORK_LAST3=1 python -m torch.distributed.launch ...`
                     """
-                    layer = nn.Identity()
+                    layer = msnn.Identity()
                 else:
                     layer = norm_layer(embed_dims[i_emb], **dd)
                 layer_name = f"norm{i_layer}"
@@ -1299,12 +1296,12 @@ 
         self.apply(self._init_weights)
 
-    def _init_weights(self, m: nn.Module) -> None:
+    def _init_weights(self, m: msnn.Cell) -> None:
         """Init. for classification"""
         if isinstance(m, nn.Linear):
             trunc_normal_(m.weight, std=0.02)
             if isinstance(m, nn.Linear) and m.bias is not None:
-                nn.init.constant_(m.bias, 0)
+                nn.init.constant_(m.bias, 0)  # 'torch.nn.init.constant_' 未在映射表(api_mapping_out_excel.json)中找到，需手动确认;
 
     @torch.jit.ignore
     def no_weight_decay(self):
@@ -1327,7 +1324,7 @@             s.grad_checkpointing = enable
 
     @torch.jit.ignore
-    def get_classifier(self) -> nn.Module:
+    def get_classifier(self) -> msnn.Cell:
         return self.head.fc
 
     def reset_classifier(self, num_classes: int, global_pool: Optional[str] = None):
@@ -1336,13 +1333,13 @@ 
     def forward_intermediates(
             self,
-            x: torch.Tensor,
+            x: ms.Tensor,
             indices: Optional[Union[int, List[int]]] = None,
             norm: bool = False,
             stop_early: bool = False,
             output_fmt: str = 'NCHW',
             intermediates_only: bool = False,
-    ) -> Union[List[torch.Tensor], Tuple[torch.Tensor, List[torch.Tensor]]]:
+    ) -> Union[List[ms.Tensor], Tuple[ms.Tensor, List[ms.Tensor]]]:
         """ Forward features that returns intermediates.
 
         Args:
@@ -1394,7 +1391,7 @@             self.reset_classifier(0, '')
         return take_indices
 
-    def forward_features(self, x: torch.Tensor) -> torch.Tensor:
+    def forward_features(self, x: ms.Tensor) -> ms.Tensor:
         # input embedding
         x = self.stem(x)
         outs = []
@@ -1411,10 +1408,10 @@         x = self.final_conv(x)
         return x
 
-    def forward_head(self, x: torch.Tensor, pre_logits: bool = False):
+    def forward_head(self, x: ms.Tensor, pre_logits: bool = False):
         return self.head(x, pre_logits=True) if pre_logits else self.head(x)
 
-    def forward(self, x: torch.Tensor) -> torch.Tensor:
+    def construct(self, x: ms.Tensor) -> ms.Tensor:
         x = self.forward_features(x)
         if self.fork_feat:
             return x
@@ -1586,7 +1583,7 @@                 # if CLIP projection, map to head.fc w/ bias = zeros
                 k = k.replace('head.proj', 'head.fc.weight')
                 v = v.T
-                out_dict['head.fc.bias'] = torch.zeros(v.shape[0])
+                out_dict['head.fc.bias'] = mint.zeros(v.shape[0])
             else:
                 k = k.replace('head.', 'head.fc.')
 
