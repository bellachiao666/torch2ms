--- pytorch+++ mindspore@@ -1,3 +1,8 @@+import mindspore as ms
+import mindspore.nn as msnn
+import mindspore.ops as msops
+import mindspore.mint as mint
+from mindspore.mint import nn, ops
 """ Quick n Simple Image Folder, Tarfile based DataSet
 
 Hacked together by / Copyright 2019, Ross Wightman
@@ -6,8 +11,8 @@ import logging
 from typing import Optional
 
-import torch
-import torch.utils.data as data
+# import torch
+# import torch.utils.data as data
 from PIL import Image
 
 from .readers import create_reader
@@ -18,6 +23,7 @@ _ERROR_RETRY = 20
 
 
+# 'torch.utils.data.Dataset' 未在映射表(api_mapping_out_excel.json)中找到，需手动确认;
 class ImageDataset(data.Dataset):
 
     def __init__(
@@ -87,6 +93,7 @@         return self.reader.filenames(basename, absolute)
 
 
+# 'torch.utils.data.IterableDataset' 未在映射表(api_mapping_out_excel.json)中找到，需手动确认;
 class IterableImageDataset(data.IterableDataset):
 
     def __init__(
@@ -167,6 +174,7 @@         return self.reader.filenames(basename, absolute)
 
 
+# 'torch.utils.data.Dataset' 未在映射表(api_mapping_out_excel.json)中找到，需手动确认;
 class AugMixDataset(torch.utils.data.Dataset):
     """Dataset wrapper to perform AugMix or other clean/augmentation mixes"""
 
