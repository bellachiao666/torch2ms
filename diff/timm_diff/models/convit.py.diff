--- pytorch+++ mindspore@@ -1,3 +1,8 @@+import mindspore as ms
+import mindspore.nn as msnn
+import mindspore.ops as msops
+import mindspore.mint as mint
+from mindspore.mint import nn, ops
 """ ConViT Model
 
 @article{d2021convit,
@@ -23,8 +28,8 @@ '''
 from typing import Optional, Union, Type, Any
 
-import torch
-import torch.nn as nn
+# import torch
+# import torch.nn as nn
 
 from timm.data import IMAGENET_DEFAULT_MEAN, IMAGENET_DEFAULT_STD
 from timm.layers import DropPath, calculate_drop_path_rates, trunc_normal_, PatchEmbed, Mlp, LayerNorm, HybridEmbed
@@ -37,7 +42,7 @@ 
 
 @register_notrace_module  # reason: FX can't symbolically trace control flow in forward method
-class GPSA(nn.Module):
+class GPSA(msnn.Cell):
     def __init__(
             self,
             dim: int,
@@ -64,10 +69,10 @@         self.proj = nn.Linear(dim, dim, **dd)
         self.pos_proj = nn.Linear(3, num_heads, **dd)
         self.proj_drop = nn.Dropout(proj_drop)
-        self.gating_param = nn.Parameter(torch.ones(self.num_heads, **dd))
-        self.rel_indices: torch.Tensor = torch.zeros(1, 1, 1, 3, **dd)  # silly torchscript hack, won't work with None
-
-    def forward(self, x):
+        self.gating_param = ms.Parameter(mint.ones(self.num_heads, **dd))
+        self.rel_indices: ms.Tensor = mint.zeros(1, 1, 1, 3, **dd)  # silly torchscript hack, won't work with None
+
+    def construct(self, x):
         B, N, C = x.shape
         if self.rel_indices is None or self.rel_indices.shape[1] != N:
             self.rel_indices = self.get_rel_indices(N)
@@ -89,7 +94,7 @@         pos_score = pos_score.softmax(dim=-1)
 
         gating = self.gating_param.view(1, -1, 1, 1)
-        attn = (1. - torch.sigmoid(gating)) * patch_score + torch.sigmoid(gating) * pos_score
+        attn = (1. - mint.sigmoid(gating)) * patch_score + mint.sigmoid(gating) * pos_score
         attn /= attn.sum(dim=-1).unsqueeze(-1)
         attn = self.attn_drop(attn)
         return attn
@@ -97,14 +102,14 @@     def get_attention_map(self, x, return_map=False):
         attn_map = self.get_attention(x).mean(0)  # average over batch
         distances = self.rel_indices.squeeze()[:, :, -1] ** .5
-        dist = torch.einsum('nm,hnm->h', (distances, attn_map)) / distances.size(0)
+        dist = mint.einsum('nm,hnm->h', (distances, attn_map)) / distances.size(0)
         if return_map:
             return dist, attn_map
         else:
             return dist
 
     def local_init(self):
-        self.v.weight.data.copy_(torch.eye(self.dim))
+        self.v.weight.data.copy_(mint.eye(self.dim))
         locality_distance = 1  # max(1,1/locality_strength**.5)
 
         kernel_size = int(self.num_heads ** .5)
@@ -117,12 +122,12 @@                 self.pos_proj.weight.data[position, 0] = 2 * (h2 - center) * locality_distance
         self.pos_proj.weight.data *= self.locality_strength
 
-    def get_rel_indices(self, num_patches: int) -> torch.Tensor:
+    def get_rel_indices(self, num_patches: int) -> ms.Tensor:
         img_size = int(num_patches ** .5)
-        rel_indices = torch.zeros(1, num_patches, num_patches, 3)
+        rel_indices = mint.zeros(size = (1, num_patches, num_patches, 3))
         ind = (
-                torch.arange(img_size, dtype=torch.float32).view(1, -1)
-                - torch.arange(img_size, dtype=torch.float32).view(-1, 1)
+                mint.arange(img_size, dtype = ms.float32).view(1, -1)
+                - mint.arange(img_size, dtype = ms.float32).view(-1, 1)
         )
         indx = ind.repeat(img_size, img_size)
         indy = ind.repeat_interleave(img_size, dim=0).repeat_interleave(img_size, dim=1)
@@ -135,7 +140,7 @@         return rel_indices.to(device=device, dtype=dtype)
 
 
-class MHSA(nn.Module):
+class MHSA(msnn.Cell):
     def __init__(
             self,
             dim: int,
@@ -166,8 +171,8 @@ 
         img_size = int(N ** .5)
         ind = (
-                torch.arange(img_size, dtype=torch.float32).view(1, -1)
-                - torch.arange(img_size, dtype=torch.float32).view(-1, 1)
+                mint.arange(img_size, dtype = ms.float32).view(1, -1)
+                - mint.arange(img_size, dtype = ms.float32).view(-1, 1)
         )
         indx = ind.repeat(img_size, img_size)
         indy = ind.repeat_interleave(img_size, dim=0).repeat_interleave(img_size, dim=1)
@@ -175,13 +180,13 @@         distances = indd ** .5
         distances = distances.to(attn_map.device, attn_map.dtype)
 
-        dist = torch.einsum('nm,hnm->h', (distances, attn_map)) / N
+        dist = mint.einsum('nm,hnm->h', (distances, attn_map)) / N
         if return_map:
             return dist, attn_map
         else:
             return dist
 
-    def forward(self, x):
+    def construct(self, x):
         B, N, C = x.shape
         qkv = self.qkv(x).reshape(B, N, 3, self.num_heads, C // self.num_heads).permute(2, 0, 3, 1, 4)
         q, k, v = qkv.unbind(0)
@@ -196,7 +201,7 @@         return x
 
 
-class Block(nn.Module):
+class Block(msnn.Cell):
 
     def __init__(
             self,
@@ -207,8 +212,8 @@             proj_drop: float = 0.,
             attn_drop: float = 0.,
             drop_path: float = 0.,
-            act_layer: Type[nn.Module] = nn.GELU,
-            norm_layer: Type[nn.Module] = LayerNorm,
+            act_layer: Type[msnn.Cell] = nn.GELU,
+            norm_layer: Type[msnn.Cell] = LayerNorm,
             use_gpsa: bool = True,
             locality_strength: float = 1.,
             device=None,
@@ -237,7 +242,7 @@                 proj_drop=proj_drop,
                 **dd,
             )
-        self.drop_path = DropPath(drop_path) if drop_path > 0. else nn.Identity()
+        self.drop_path = DropPath(drop_path) if drop_path > 0. else msnn.Identity()
         self.norm2 = norm_layer(dim, **dd)
         mlp_hidden_dim = int(dim * mlp_ratio)
         self.mlp = Mlp(
@@ -248,13 +253,13 @@             **dd,
         )
 
-    def forward(self, x):
+    def construct(self, x):
         x = x + self.drop_path(self.attn(self.norm1(x)))
         x = x + self.drop_path(self.mlp(self.norm2(x)))
         return x
 
 
-class ConVit(nn.Module):
+class ConVit(msnn.Cell):
     """ Vision Transformer with support for patch or hybrid CNN input stage
     """
 
@@ -276,7 +281,7 @@             attn_drop_rate: float = 0.,
             drop_path_rate: float = 0.,
             hybrid_backbone: Optional[Any] = None,
-            norm_layer: Type[nn.Module] = LayerNorm,
+            norm_layer: Type[msnn.Cell] = LayerNorm,
             local_up_to_layer: int = 3,
             locality_strength: float = 1.,
             use_pos_embed: bool = True,
@@ -313,15 +318,15 @@         num_patches = self.patch_embed.num_patches
         self.num_patches = num_patches
 
-        self.cls_token = nn.Parameter(torch.zeros(1, 1, embed_dim, **dd))
-        self.pos_drop = nn.Dropout(p=pos_drop_rate)
+        self.cls_token = ms.Parameter(mint.zeros(1, 1, embed_dim, **dd))
+        self.pos_drop = nn.Dropout(p = pos_drop_rate)
 
         if self.use_pos_embed:
-            self.pos_embed = nn.Parameter(torch.zeros(1, num_patches, embed_dim, **dd))
+            self.pos_embed = ms.Parameter(mint.zeros(1, num_patches, embed_dim, **dd))
             trunc_normal_(self.pos_embed, std=.02)
 
         dpr = calculate_drop_path_rates(drop_path_rate, depth)  # stochastic depth decay rule
-        self.blocks = nn.ModuleList([
+        self.blocks = msnn.CellList([
             Block(
                 dim=embed_dim,
                 num_heads=num_heads,
@@ -340,7 +345,7 @@         # Classifier head
         self.feature_info = [dict(num_chs=embed_dim, reduction=0, module='head')]
         self.head_drop = nn.Dropout(drop_rate)
-        self.head = nn.Linear(embed_dim, num_classes, **dd) if num_classes > 0 else nn.Identity()
+        self.head = nn.Linear(embed_dim, num_classes, **dd) if num_classes > 0 else msnn.Identity()
 
         trunc_normal_(self.cls_token, std=.02)
         self.apply(self._init_weights)
@@ -352,10 +357,10 @@         if isinstance(m, nn.Linear):
             trunc_normal_(m.weight, std=.02)
             if isinstance(m, nn.Linear) and m.bias is not None:
-                nn.init.constant_(m.bias, 0)
+                nn.init.constant_(m.bias, 0)  # 'torch.nn.init.constant_' 未在映射表(api_mapping_out_excel.json)中找到，需手动确认;
         elif isinstance(m, nn.LayerNorm):
-            nn.init.constant_(m.bias, 0)
-            nn.init.constant_(m.weight, 1.0)
+            nn.init.constant_(m.bias, 0)  # 'torch.nn.init.constant_' 未在映射表(api_mapping_out_excel.json)中找到，需手动确认;
+            nn.init.constant_(m.weight, 1.0)  # 'torch.nn.init.constant_' 未在映射表(api_mapping_out_excel.json)中找到，需手动确认;
 
     @torch.jit.ignore
     def no_weight_decay(self):
@@ -373,7 +378,7 @@         assert not enable, 'gradient checkpointing not supported'
 
     @torch.jit.ignore
-    def get_classifier(self) -> nn.Module:
+    def get_classifier(self) -> msnn.Cell:
         return self.head
 
     def reset_classifier(self, num_classes: int, global_pool: Optional[str] = None):
@@ -381,7 +386,7 @@         if global_pool is not None:
             assert global_pool in ('', 'token', 'avg')
             self.global_pool = global_pool
-        self.head = nn.Linear(self.embed_dim, num_classes) if num_classes > 0 else nn.Identity()
+        self.head = nn.Linear(self.embed_dim, num_classes) if num_classes > 0 else msnn.Identity()
 
     def forward_features(self, x):
         x = self.patch_embed(x)
@@ -391,7 +396,7 @@         cls_tokens = self.cls_token.expand(x.shape[0], -1, -1)
         for u, blk in enumerate(self.blocks):
             if u == self.local_up_to_layer:
-                x = torch.cat((cls_tokens, x), dim=1)
+                x = mint.cat((cls_tokens, x), dim = 1)
             x = blk(x)
         x = self.norm(x)
         return x
@@ -402,7 +407,7 @@         x = self.head_drop(x)
         return x if pre_logits else self.head(x)
 
-    def forward(self, x):
+    def construct(self, x):
         x = self.forward_features(x)
         x = self.forward_head(x)
         return x
