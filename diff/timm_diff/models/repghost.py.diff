--- pytorch+++ mindspore@@ -1,3 +1,8 @@+import mindspore as ms
+import mindspore.nn as msnn
+import mindspore.ops as msops
+import mindspore.mint as mint
+from mindspore.mint import nn, ops
 """
 An implementation of RepGhostNet Model as defined in:
 RepGhost: A Hardware-Efficient Ghost Module via Re-parameterization. https://arxiv.org/abs/2211.06088
@@ -8,9 +13,8 @@ from functools import partial
 from typing import List, Optional, Tuple, Union, Type
 
-import torch
-import torch.nn as nn
-import torch.nn.functional as F
+# import torch
+# import torch.nn as nn
 
 from timm.data import IMAGENET_DEFAULT_MEAN, IMAGENET_DEFAULT_STD
 from timm.layers import SelectAdaptivePool2d, Linear, make_divisible
@@ -26,7 +30,7 @@ _SE_LAYER = partial(SqueezeExcite, gate_layer='hard_sigmoid', rd_round_fn=partial(make_divisible, divisor=4))
 
 
-class RepGhostModule(nn.Module):
+class RepGhostModule(msnn.Cell):
     def __init__(
             self,
             in_chs: int,
@@ -45,29 +49,30 @@         init_chs = out_chs
         new_chs = out_chs
 
-        self.primary_conv = nn.Sequential(
+        self.primary_conv = msnn.SequentialCell(
+            [
             nn.Conv2d(in_chs, init_chs, kernel_size, stride, kernel_size // 2, bias=False, **dd),
             nn.BatchNorm2d(init_chs, **dd),
-            nn.ReLU(inplace=True) if relu else nn.Identity(),
-        )
+            nn.ReLU() if relu else msnn.Identity()
+        ])  # 存在 *args/**kwargs，需手动确认参数映射;; 'torch.nn.ReLU':没有对应的mindspore参数 'inplace' (position 0);
 
         fusion_conv = []
         fusion_bn = []
         if reparam:
-            fusion_conv.append(nn.Identity())
-            fusion_bn.append(nn.BatchNorm2d(init_chs, **dd))
-
-        self.fusion_conv = nn.Sequential(*fusion_conv)
-        self.fusion_bn = nn.Sequential(*fusion_bn)
-
-        self.cheap_operation = nn.Sequential(
+            fusion_conv.append(msnn.Identity())
+            fusion_bn.append(nn.BatchNorm2d(init_chs, **dd))  # 存在 *args/**kwargs，需手动确认参数映射;
+
+        self.fusion_conv = msnn.SequentialCell(*fusion_conv)  # 存在 *args/**kwargs，未转换，需手动确认参数映射;
+        self.fusion_bn = msnn.SequentialCell(*fusion_bn)  # 存在 *args/**kwargs，未转换，需手动确认参数映射;
+
+        self.cheap_operation = msnn.SequentialCell(
+            [
             nn.Conv2d(init_chs, new_chs, dw_size, 1, dw_size//2, groups=init_chs, bias=False, **dd),
-            nn.BatchNorm2d(new_chs, **dd),
-            # nn.ReLU(inplace=True) if relu else nn.Identity(),
-        )
-        self.relu = nn.ReLU(inplace=False) if relu else nn.Identity()
-
-    def forward(self, x):
+            nn.BatchNorm2d(new_chs, **dd)
+        ])  # 存在 *args/**kwargs，需手动确认参数映射;
+        self.relu = nn.ReLU() if relu else msnn.Identity()  # 'torch.nn.ReLU':没有对应的mindspore参数 'inplace' (position 0);
+
+    def construct(self, x):
         x1 = self.primary_conv(x)
         x2 = self.cheap_operation(x1)
         for conv, bn in zip(self.fusion_conv, self.fusion_bn):
@@ -87,7 +92,7 @@         if kernel1x1 is None:
             return 0
         else:
-            return torch.nn.functional.pad(kernel1x1, [1, 1, 1, 1])
+            return nn.functional.pad(kernel1x1, [1, 1, 1, 1])
 
     @staticmethod
     def _fuse_bn_tensor(conv, bn, in_channels=None, device=None):
@@ -97,8 +102,8 @@             kernel = conv.weight
             assert conv.bias is None
         else:
-            assert isinstance(conv, nn.Identity)
-            kernel = torch.ones(in_channels, 1, 1, 1, device=device)
+            assert isinstance(conv, msnn.Identity)
+            kernel = mint.ones(in_channels, 1, 1, 1, device=device)
 
         if isinstance(bn, nn.BatchNorm2d):
             running_mean = bn.running_mean
@@ -109,8 +114,8 @@             std = (running_var + eps).sqrt()
             t = (gamma / std).reshape(-1, 1, 1, 1)
             return kernel * t, beta - running_mean * gamma / std
-        assert isinstance(bn, nn.Identity)
-        return kernel, torch.zeros(in_channels).to(kernel.device)
+        assert isinstance(bn, msnn.Identity)
+        return kernel, mint.zeros(in_channels).to(kernel.device)
 
     def switch_to_deploy(self):
         if len(self.fusion_conv) == 0 and len(self.fusion_bn) == 0:
@@ -125,7 +130,7 @@             dilation=self.cheap_operation[0].dilation,
             groups=self.cheap_operation[0].groups,
             bias=True,
-            **dd)
+            **dd)  # 存在 *args/**kwargs，需手动确认参数映射;
         self.cheap_operation.weight.data = kernel
         self.cheap_operation.bias.data = bias
         self.__delattr__('fusion_conv')
@@ -137,7 +142,7 @@         self.switch_to_deploy()
 
 
-class RepGhostBottleneck(nn.Module):
+class RepGhostBottleneck(msnn.Cell):
     """ RepGhost bottleneck w/ optional SE"""
 
     def __init__(
@@ -147,7 +152,7 @@             out_chs: int,
             dw_kernel_size: int = 3,
             stride: int = 1,
-            act_layer: Type[nn.Module] = nn.ReLU,
+            act_layer: Type[msnn.Cell] = nn.ReLU,
             se_ratio: float = 0.,
             reparam: bool = True,
             device=None,
@@ -159,7 +164,7 @@         self.stride = stride
 
         # Point-wise expansion
-        self.ghost1 = RepGhostModule(in_chs, mid_chs, relu=True, reparam=reparam, **dd)
+        self.ghost1 = RepGhostModule(in_chs, mid_chs, relu=True, reparam=reparam, **dd)  # 存在 *args/**kwargs，未转换，需手动确认参数映射;
 
         # Depth-wise convolution
         if self.stride > 1:
@@ -172,23 +177,24 @@                 groups=mid_chs,
                 bias=False,
                 **dd,
-            )
-            self.bn_dw = nn.BatchNorm2d(mid_chs, **dd)
+            )  # 存在 *args/**kwargs，需手动确认参数映射;
+            self.bn_dw = nn.BatchNorm2d(mid_chs, **dd)  # 存在 *args/**kwargs，需手动确认参数映射;
         else:
             self.conv_dw = None
             self.bn_dw = None
 
         # Squeeze-and-excitation
-        self.se = _SE_LAYER(mid_chs, rd_ratio=se_ratio, **dd) if has_se else None
+        self.se = _SE_LAYER(mid_chs, rd_ratio=se_ratio, **dd) if has_se else None  # 存在 *args/**kwargs，未转换，需手动确认参数映射;
 
         # Point-wise linear projection
-        self.ghost2 = RepGhostModule(mid_chs, out_chs, relu=False, reparam=reparam, **dd)
+        self.ghost2 = RepGhostModule(mid_chs, out_chs, relu=False, reparam=reparam, **dd)  # 存在 *args/**kwargs，未转换，需手动确认参数映射;
 
         # shortcut
         if in_chs == out_chs and self.stride == 1:
-            self.shortcut = nn.Sequential()
-        else:
-            self.shortcut = nn.Sequential(
+            self.shortcut = msnn.SequentialCell()
+        else:
+            self.shortcut = msnn.SequentialCell(
+                [
                 nn.Conv2d(
                     in_chs,
                     in_chs,
@@ -201,10 +207,10 @@                 ),
                 nn.BatchNorm2d(in_chs, **dd),
                 nn.Conv2d(in_chs, out_chs, 1, stride=1, padding=0, bias=False, **dd),
-                nn.BatchNorm2d(out_chs, **dd),
-            )
-
-    def forward(self, x):
+                nn.BatchNorm2d(out_chs, **dd)
+            ])  # 存在 *args/**kwargs，需手动确认参数映射;
+
+    def construct(self, x):
         shortcut = x
 
         # 1st ghost bottleneck
@@ -226,7 +232,7 @@         return x
 
 
-class RepGhostNet(nn.Module):
+class RepGhostNet(msnn.Cell):
     def __init__(
             self,
             cfgs: List[List[List]],
@@ -252,14 +258,14 @@ 
         # building first layer
         stem_chs = make_divisible(16 * width, 4)
-        self.conv_stem = nn.Conv2d(in_chans, stem_chs, 3, 2, 1, bias=False, **dd)
+        self.conv_stem = nn.Conv2d(in_chans, stem_chs, 3, 2, 1, bias=False, **dd)  # 存在 *args/**kwargs，需手动确认参数映射;
         self.feature_info.append(dict(num_chs=stem_chs, reduction=2, module=f'conv_stem'))
-        self.bn1 = nn.BatchNorm2d(stem_chs, **dd)
-        self.act1 = nn.ReLU(inplace=True)
+        self.bn1 = nn.BatchNorm2d(stem_chs, **dd)  # 存在 *args/**kwargs，需手动确认参数映射;
+        self.act1 = nn.ReLU()  # 'torch.nn.ReLU':没有对应的mindspore参数 'inplace' (position 0);
         prev_chs = stem_chs
 
         # building inverted residual blocks
-        stages = nn.ModuleList([])
+        stages = msnn.CellList([])
         block = RepGhostBottleneck
         stage_idx = 0
         net_stride = 2
@@ -269,31 +275,33 @@             for k, exp_size, c, se_ratio, s in cfg:
                 out_chs = make_divisible(c * width, 4)
                 mid_chs = make_divisible(exp_size * width, 4)
-                layers.append(block(prev_chs, mid_chs, out_chs, k, s, se_ratio=se_ratio, reparam=reparam, **dd))
+                layers.append(block(prev_chs, mid_chs, out_chs, k, s, se_ratio=se_ratio, reparam=reparam, **dd))  # 存在 *args/**kwargs，未转换，需手动确认参数映射;
                 prev_chs = out_chs
             if s > 1:
                 net_stride *= 2
                 self.feature_info.append(dict(
                     num_chs=prev_chs, reduction=net_stride, module=f'blocks.{stage_idx}'))
-            stages.append(nn.Sequential(*layers))
+            stages.append(msnn.SequentialCell(*layers))  # 存在 *args/**kwargs，未转换，需手动确认参数映射;
             stage_idx += 1
 
         out_chs = make_divisible(exp_size * width * 2, 4)
-        stages.append(nn.Sequential(ConvBnAct(prev_chs, out_chs, 1, **dd)))
+        stages.append(msnn.SequentialCell([
+            ConvBnAct(prev_chs, out_chs, 1, **dd)
+        ]))  # 存在 *args/**kwargs，未转换，需手动确认参数映射;
         self.pool_dim = prev_chs = out_chs
 
-        self.blocks = nn.Sequential(*stages)
+        self.blocks = msnn.SequentialCell(*stages)  # 存在 *args/**kwargs，未转换，需手动确认参数映射;
 
         # building last several layers
         self.num_features = prev_chs
         self.head_hidden_size = out_chs = 1280
         self.global_pool = SelectAdaptivePool2d(pool_type=global_pool)
-        self.conv_head = nn.Conv2d(prev_chs, out_chs, 1, 1, 0, bias=True, **dd)
-        self.act2 = nn.ReLU(inplace=True)
-        self.flatten = nn.Flatten(1) if global_pool else nn.Identity()  # don't flatten if pooling disabled
-        self.classifier = Linear(out_chs, num_classes, **dd) if num_classes > 0 else nn.Identity()
-
-    @torch.jit.ignore
+        self.conv_head = nn.Conv2d(prev_chs, out_chs, 1, 1, 0, bias=True, **dd)  # 存在 *args/**kwargs，需手动确认参数映射;
+        self.act2 = nn.ReLU()  # 'torch.nn.ReLU':没有对应的mindspore参数 'inplace' (position 0);
+        self.flatten = mint.flatten(1) if global_pool else msnn.Identity()  # don't flatten if pooling disabled
+        self.classifier = Linear(out_chs, num_classes, **dd) if num_classes > 0 else msnn.Identity()  # 存在 *args/**kwargs，未转换，需手动确认参数映射;
+
+    @ms.jit
     def group_matcher(self, coarse=False):
         matcher = dict(
             stem=r'^conv_stem|bn1',
@@ -304,12 +312,12 @@         )
         return matcher
 
-    @torch.jit.ignore
+    @ms.jit
     def set_grad_checkpointing(self, enable=True):
         self.grad_checkpointing = enable
 
-    @torch.jit.ignore
-    def get_classifier(self) -> nn.Module:
+    @ms.jit
+    def get_classifier(self) -> msnn.Cell:
         return self.classifier
 
     def reset_classifier(self, num_classes: int, global_pool: Optional[str] = None):
@@ -317,24 +325,24 @@         if global_pool is not None:
             # NOTE: cannot meaningfully change pooling of efficient head after creation
             self.global_pool = SelectAdaptivePool2d(pool_type=global_pool)
-            self.flatten = nn.Flatten(1) if global_pool else nn.Identity()  # don't flatten if pooling disabled
+            self.flatten = mint.flatten(1) if global_pool else msnn.Identity()  # don't flatten if pooling disabled
         if num_classes > 0:
             device = self.classifier.weight.device if hasattr(self.classifier, 'weight') else None
             dtype = self.classifier.weight.dtype if hasattr(self.classifier, 'weight') else None
             dd = {'device': device, 'dtype': dtype}
-            self.classifier = Linear(self.head_hidden_size, num_classes, **dd)
-        else:
-            self.classifier = nn.Identity()
+            self.classifier = Linear(self.head_hidden_size, num_classes, **dd)  # 存在 *args/**kwargs，未转换，需手动确认参数映射;
+        else:
+            self.classifier = msnn.Identity()
 
     def forward_intermediates(
             self,
-            x: torch.Tensor,
+            x: ms.Tensor,
             indices: Optional[Union[int, List[int]]] = None,
             norm: bool = False,
             stop_early: bool = False,
             output_fmt: str = 'NCHW',
             intermediates_only: bool = False,
-    ) -> Union[List[torch.Tensor], Tuple[torch.Tensor, List[torch.Tensor]]]:
+    ) -> Union[List[ms.Tensor], Tuple[ms.Tensor, List[ms.Tensor]]]:
         """ Forward features that returns intermediates.
 
         Args:
@@ -361,12 +369,14 @@             intermediates.append(x)
         x = self.bn1(x)
         x = self.act1(x)
+        # 'torch.jit.is_scripting' 未在映射表(api_mapping_out_excel.json)中找到，需手动确认;
         if torch.jit.is_scripting() or not stop_early:  # can't slice blocks in torchscript
             stages = self.blocks
         else:
             stages = self.blocks[:max_index + 1]
 
         for feat_idx, stage in enumerate(stages, start=1):
+            # 'torch.jit.is_scripting' 未在映射表(api_mapping_out_excel.json)中找到，需手动确认;
             if self.grad_checkpointing and not torch.jit.is_scripting():
                 x = checkpoint_seq(stage, x)
             else:
@@ -399,6 +409,7 @@         x = self.conv_stem(x)
         x = self.bn1(x)
         x = self.act1(x)
+        # 'torch.jit.is_scripting' 未在映射表(api_mapping_out_excel.json)中找到，需手动确认;
         if self.grad_checkpointing and not torch.jit.is_scripting():
             x = checkpoint_seq(self.blocks, x, flatten=True)
         else:
@@ -411,10 +422,10 @@         x = self.act2(x)
         x = self.flatten(x)
         if self.drop_rate > 0.:
-            x = F.dropout(x, p=self.drop_rate, training=self.training)
+            x = nn.functional.dropout(x, p = self.drop_rate, training = self.training)
         return x if pre_logits else self.classifier(x)
 
-    def forward(self, x):
+    def construct(self, x):
         x = self.forward_features(x)
         x = self.forward_head(x)
         return x
@@ -423,7 +434,7 @@         repghost_model_convert(self, do_copy=False)
 
 
-def repghost_model_convert(model: torch.nn.Module, save_path=None, do_copy=True):
+def repghost_model_convert(model: msnn.Cell, save_path=None, do_copy=True):
     """
     taken from from https://github.com/DingXiaoH/RepVGG/blob/main/repvgg.py
     """
@@ -433,7 +444,7 @@         if hasattr(module, 'switch_to_deploy'):
             module.switch_to_deploy()
     if save_path is not None:
-        torch.save(model.state_dict(), save_path)
+        torch.save(model.state_dict(), save_path)  # 'torch.save' 未在映射表(api_mapping_out_excel.json)中找到，需手动确认;
     return model
 
 
@@ -471,14 +482,14 @@         cfgs=cfgs,
         width=width,
         **kwargs,
-    )
+    )  # 存在 *args/**kwargs，未转换，需手动确认参数映射;
     return build_model_with_cfg(
         RepGhostNet,
         variant,
         pretrained,
         feature_cfg=dict(flatten_sequential=True),
         **model_kwargs,
-    )
+    )  # 存在 *args/**kwargs，未转换，需手动确认参数映射;
 
 
 def _cfg(url='', **kwargs):
@@ -531,53 +542,53 @@ @register_model
 def repghostnet_050(pretrained=False, **kwargs) -> RepGhostNet:
     """ RepGhostNet-0.5x """
-    model = _create_repghostnet('repghostnet_050', width=0.5, pretrained=pretrained, **kwargs)
+    model = _create_repghostnet('repghostnet_050', width=0.5, pretrained=pretrained, **kwargs)  # 存在 *args/**kwargs，未转换，需手动确认参数映射;
     return model
 
 
 @register_model
 def repghostnet_058(pretrained=False, **kwargs) -> RepGhostNet:
     """ RepGhostNet-0.58x """
-    model = _create_repghostnet('repghostnet_058', width=0.58, pretrained=pretrained, **kwargs)
+    model = _create_repghostnet('repghostnet_058', width=0.58, pretrained=pretrained, **kwargs)  # 存在 *args/**kwargs，未转换，需手动确认参数映射;
     return model
 
 
 @register_model
 def repghostnet_080(pretrained=False, **kwargs) -> RepGhostNet:
     """ RepGhostNet-0.8x """
-    model = _create_repghostnet('repghostnet_080', width=0.8, pretrained=pretrained, **kwargs)
+    model = _create_repghostnet('repghostnet_080', width=0.8, pretrained=pretrained, **kwargs)  # 存在 *args/**kwargs，未转换，需手动确认参数映射;
     return model
 
 
 @register_model
 def repghostnet_100(pretrained=False, **kwargs) -> RepGhostNet:
     """ RepGhostNet-1.0x """
-    model = _create_repghostnet('repghostnet_100', width=1.0, pretrained=pretrained, **kwargs)
+    model = _create_repghostnet('repghostnet_100', width=1.0, pretrained=pretrained, **kwargs)  # 存在 *args/**kwargs，未转换，需手动确认参数映射;
     return model
 
 
 @register_model
 def repghostnet_111(pretrained=False, **kwargs) -> RepGhostNet:
     """ RepGhostNet-1.11x """
-    model = _create_repghostnet('repghostnet_111', width=1.11, pretrained=pretrained, **kwargs)
+    model = _create_repghostnet('repghostnet_111', width=1.11, pretrained=pretrained, **kwargs)  # 存在 *args/**kwargs，未转换，需手动确认参数映射;
     return model
 
 @register_model
 def repghostnet_130(pretrained=False, **kwargs) -> RepGhostNet:
     """ RepGhostNet-1.3x """
-    model = _create_repghostnet('repghostnet_130', width=1.3, pretrained=pretrained, **kwargs)
+    model = _create_repghostnet('repghostnet_130', width=1.3, pretrained=pretrained, **kwargs)  # 存在 *args/**kwargs，未转换，需手动确认参数映射;
     return model
 
 
 @register_model
 def repghostnet_150(pretrained=False, **kwargs) -> RepGhostNet:
     """ RepGhostNet-1.5x """
-    model = _create_repghostnet('repghostnet_150', width=1.5, pretrained=pretrained, **kwargs)
+    model = _create_repghostnet('repghostnet_150', width=1.5, pretrained=pretrained, **kwargs)  # 存在 *args/**kwargs，未转换，需手动确认参数映射;
     return model
 
 
 @register_model
 def repghostnet_200(pretrained=False, **kwargs) -> RepGhostNet:
     """ RepGhostNet-2.0x """
-    model = _create_repghostnet('repghostnet_200', width=2.0, pretrained=pretrained, **kwargs)
-    return model
+    model = _create_repghostnet('repghostnet_200', width=2.0, pretrained=pretrained, **kwargs)  # 存在 *args/**kwargs，未转换，需手动确认参数映射;
+    return model
