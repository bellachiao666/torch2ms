--- pytorch+++ mindspore@@ -1,3 +1,8 @@+import mindspore as ms
+import mindspore.nn as msnn
+import mindspore.ops as msops
+import mindspore.mint as mint
+from mindspore.mint import nn, ops
 """ ReXNet
 
 A PyTorch impl of `ReXNet: Diminishing Representational Bottleneck on Convolutional Neural Network` -
@@ -14,8 +19,8 @@ from math import ceil
 from typing import Any, Dict, List, Optional, Tuple, Type, Union
 
-import torch
-import torch.nn as nn
+# import torch
+# import torch.nn as nn
 
 from timm.data import IMAGENET_DEFAULT_MEAN, IMAGENET_DEFAULT_STD
 from timm.layers import ClassifierHead, create_act_layer, ConvNormAct, DropPath, make_divisible, SEModule
@@ -31,7 +36,7 @@ SEWithNorm = partial(SEModule, norm_layer=nn.BatchNorm2d)
 
 
-class LinearBottleneck(nn.Module):
+class LinearBottleneck(msnn.Cell):
     """Linear bottleneck block for ReXNet.
 
     A mobile inverted residual bottleneck block as used in MobileNetV2 and subsequent models.
@@ -109,7 +114,7 @@         """
         return self.conv_dw.out_channels if exp else self.out_channels
 
-    def forward(self, x: torch.Tensor) -> torch.Tensor:
+    def construct(self, x: ms.Tensor) -> ms.Tensor:
         """Forward pass.
 
         Args:
@@ -129,7 +134,7 @@         if self.use_shortcut:
             if self.drop_path is not None:
                 x = self.drop_path(x)
-            x = torch.cat([x[:, 0:self.in_channels] + shortcut, x[:, self.in_channels:]], dim=1)
+            x = mint.cat([x[:, 0:self.in_channels] + shortcut, x[:, self.in_channels:]], dim = 1)
         return x
 
 
@@ -240,7 +245,7 @@     return features, feature_info
 
 
-class RexNet(nn.Module):
+class RexNet(msnn.Cell):
     """ReXNet model architecture.
 
     Based on `ReXNet: Diminishing Representational Bottleneck on Convolutional Neural Network`
@@ -308,7 +313,9 @@             **dd,
         )
         self.num_features = self.head_hidden_size = features[-1].out_channels
-        self.features = nn.Sequential(*features)
+        self.features = msnn.SequentialCell([
+            features
+        ])
 
         self.head = ClassifierHead(self.num_features, num_classes, global_pool, drop_rate, **dd)
 
@@ -339,6 +346,7 @@         """
         self.grad_checkpointing = enable
 
+    # 类型标注 'torch.nn.Module' 未在映射表(api_mapping_out_excel.json)中找到，需手动确认;
     @torch.jit.ignore
     def get_classifier(self) -> nn.Module:
         """Get the classifier module.
@@ -365,7 +373,7 @@ 
     def forward_intermediates(
             self,
-            x: torch.Tensor,
+            x: ms.Tensor,
             indices: Optional[Union[int, List[int]]] = None,
             norm: bool = False,
             stop_early: bool = False,
@@ -435,7 +443,7 @@             self.reset_classifier(0, '')
         return take_indices
 
-    def forward_features(self, x: torch.Tensor) -> torch.Tensor:
+    def forward_features(self, x: ms.Tensor) -> ms.Tensor:
         """Forward pass through feature extraction layers.
 
         Args:
@@ -451,7 +459,7 @@             x = self.features(x)
         return x
 
-    def forward_head(self, x: torch.Tensor, pre_logits: bool = False) -> torch.Tensor:
+    def forward_head(self, x: ms.Tensor, pre_logits: bool = False) -> ms.Tensor:
         """Forward pass through head.
 
         Args:
@@ -463,7 +471,7 @@         """
         return self.head(x, pre_logits=pre_logits) if pre_logits else self.head(x)
 
-    def forward(self, x: torch.Tensor) -> torch.Tensor:
+    def construct(self, x: ms.Tensor) -> ms.Tensor:
         """Forward pass.
 
         Args:
