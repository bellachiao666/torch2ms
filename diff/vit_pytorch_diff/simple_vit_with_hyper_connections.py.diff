--- pytorch+++ mindspore@@ -1,14 +1,16 @@+import mindspore as ms
+import mindspore.nn as msnn
+import mindspore.ops as msops
+import mindspore.mint as mint
+from mindspore.mint import nn, ops
 """
 ViT + Hyper-Connections + Register Tokens
 https://arxiv.org/abs/2409.19606
 """
-
-import torch
-from torch import nn, tensor
-from torch.nn import Module, ModuleList
+# from torch import nn, tensor
 
 from einops import rearrange, repeat, reduce, einsum, pack, unpack
-from einops.layers.torch import Rearrange
+# from einops.layers.torch import Rearrange
 
 # b - batch, h - heads, n - sequence, e - expansion rate / residual streams, d - feature dimension
 
@@ -17,20 +19,20 @@ def pair(t):
     return t if isinstance(t, tuple) else (t, t)
 
-def posemb_sincos_2d(h, w, dim, temperature: int = 10000, dtype = torch.float32):
-    y, x = torch.meshgrid(torch.arange(h), torch.arange(w), indexing="ij")
+def posemb_sincos_2d(h, w, dim, temperature: int = 10000, dtype = ms.float32):
+    y, x = mint.meshgrid(mint.arange(h), mint.arange(w), indexing = "ij")
     assert (dim % 4) == 0, "feature dimension must be multiple of 4 for sincos emb"
-    omega = torch.arange(dim // 4) / (dim // 4 - 1)
+    omega = mint.arange(dim // 4) / (dim // 4 - 1)
     omega = 1.0 / (temperature ** omega)
 
     y = y.flatten()[:, None] * omega[None, :]
     x = x.flatten()[:, None] * omega[None, :]
-    pe = torch.cat((x.sin(), x.cos(), y.sin(), y.cos()), dim=1)
+    pe = mint.cat((x.sin(), x.cos(), y.sin(), y.cos()), dim = 1)
     return pe.type(dtype)
 
 # hyper connections
 
-class HyperConnection(Module):
+class HyperConnection(msnn.Cell):
     def __init__(
         self,
         dim,
@@ -45,17 +47,17 @@         self.num_residual_streams = num_residual_streams
         self.layer_index = layer_index
 
-        self.static_beta = nn.Parameter(torch.ones(num_residual_streams))
-
-        init_alpha0 = torch.zeros((num_residual_streams, 1))
+        self.static_beta = ms.Parameter(mint.ones(num_residual_streams))
+
+        init_alpha0 = mint.zeros((num_residual_streams, 1))
         init_alpha0[layer_index % num_residual_streams, 0] = 1.
 
-        self.static_alpha = nn.Parameter(torch.cat([init_alpha0, torch.eye(num_residual_streams)], dim = 1))
-
-        self.dynamic_alpha_fn = nn.Parameter(torch.zeros(dim, num_residual_streams + 1))
-        self.dynamic_alpha_scale = nn.Parameter(tensor(1e-2))
-        self.dynamic_beta_fn = nn.Parameter(torch.zeros(dim))
-        self.dynamic_beta_scale = nn.Parameter(tensor(1e-2))
+        self.static_alpha = ms.Parameter(mint.cat([init_alpha0, mint.eye(num_residual_streams)], dim = 1))
+
+        self.dynamic_alpha_fn = ms.Parameter(mint.zeros(size = (dim, num_residual_streams + 1)))
+        self.dynamic_alpha_scale = ms.Parameter(tensor(1e-2))  # 'torch.tensor' 未在映射表(api_mapping_out_excel.json)中找到，需手动确认;
+        self.dynamic_beta_fn = ms.Parameter(mint.zeros(dim))
+        self.dynamic_beta_scale = ms.Parameter(tensor(1e-2))  # 'torch.tensor' 未在映射表(api_mapping_out_excel.json)中找到，需手动确认;
 
     def width_connection(self, residuals):
         normed = self.norm(residuals)
@@ -85,19 +87,20 @@ 
 # classes
 
-class FeedForward(Module):
+class FeedForward(msnn.Cell):
     def __init__(self, dim, hidden_dim):
         super().__init__()
-        self.net = nn.Sequential(
+        self.net = msnn.SequentialCell(
+            [
             nn.LayerNorm(dim),
             nn.Linear(dim, hidden_dim),
             nn.GELU(),
-            nn.Linear(hidden_dim, dim),
-        )
-    def forward(self, x):
+            nn.Linear(hidden_dim, dim)
+        ])
+    def construct(self, x):
         return self.net(x)
 
-class Attention(Module):
+class Attention(msnn.Cell):
     def __init__(self, dim, heads = 8, dim_head = 64):
         super().__init__()
         inner_dim = dim_head *  heads
@@ -110,38 +113,38 @@         self.to_qkv = nn.Linear(dim, inner_dim * 3, bias = False)
         self.to_out = nn.Linear(inner_dim, dim, bias = False)
 
-    def forward(self, x):
+    def construct(self, x):
         x = self.norm(x)
 
         qkv = self.to_qkv(x).chunk(3, dim = -1)
         q, k, v = map(lambda t: rearrange(t, 'b n (h d) -> b h n d', h = self.heads), qkv)
 
-        dots = torch.matmul(q, k.transpose(-1, -2)) * self.scale
+        dots = mint.matmul(q, k.transpose(-1, -2)) * self.scale
 
         attn = self.attend(dots)
 
-        out = torch.matmul(attn, v)
+        out = mint.matmul(attn, v)
         out = rearrange(out, 'b h n d -> b n (h d)')
         return self.to_out(out)
 
-class Transformer(Module):
+class Transformer(msnn.Cell):
     def __init__(self, dim, depth, heads, dim_head, mlp_dim, num_residual_streams):
         super().__init__()
 
         self.num_residual_streams = num_residual_streams
 
         self.norm = nn.LayerNorm(dim)
-        self.layers = ModuleList([])
+        self.layers = msnn.CellList([])
 
         for layer_index in range(depth):
-            self.layers.append(nn.ModuleList([
+            self.layers.append(msnn.CellList([
                 HyperConnection(dim, num_residual_streams, layer_index),
                 Attention(dim, heads = heads, dim_head = dim_head),
                 HyperConnection(dim, num_residual_streams, layer_index),
                 FeedForward(dim, mlp_dim)
             ]))
 
-    def forward(self, x):
+    def construct(self, x):
 
         x = repeat(x, 'b n d -> b n e d', e = self.num_residual_streams)
 
@@ -163,7 +166,7 @@ 
         return self.norm(x)
 
-class SimpleViT(nn.Module):
+class SimpleViT(msnn.Cell):
     def __init__(self, *, image_size, patch_size, num_classes, dim, depth, heads, mlp_dim, num_residual_streams, num_register_tokens = 4, channels = 3, dim_head = 64):
         super().__init__()
         image_height, image_width = pair(image_size)
@@ -173,14 +176,15 @@ 
         patch_dim = channels * patch_height * patch_width
 
-        self.to_patch_embedding = nn.Sequential(
+        self.to_patch_embedding = msnn.SequentialCell(
+            [
             Rearrange("b c (h p1) (w p2) -> b (h w) (p1 p2 c)", p1 = patch_height, p2 = patch_width),
             nn.LayerNorm(patch_dim),
             nn.Linear(patch_dim, dim),
-            nn.LayerNorm(dim),
-        )
-
-        self.register_tokens = nn.Parameter(torch.randn(num_register_tokens, dim))
+            nn.LayerNorm(dim)
+        ])
+
+        self.register_tokens = ms.Parameter(mint.randn(size = (num_register_tokens, dim)))
 
         self.pos_embedding = posemb_sincos_2d(
             h = image_height // patch_height,
@@ -191,11 +195,11 @@         self.transformer = Transformer(dim, depth, heads, dim_head, mlp_dim, num_residual_streams)
 
         self.pool = "mean"
-        self.to_latent = nn.Identity()
+        self.to_latent = msnn.Identity()
 
         self.linear_head = nn.Linear(dim, num_classes)
 
-    def forward(self, img):
+    def construct(self, img):
         batch, device = img.shape[0], img.device
 
         x = self.to_patch_embedding(img)
@@ -228,6 +232,6 @@         num_residual_streams = 8
     )
 
-    images = torch.randn(3, 3, 256, 256)
+    images = mint.randn(size = (3, 3, 256, 256))
 
     logits = vit(images)
